# Backend (Sprint 0 shell)

Run locally after installing dependencies:

```bash
uvicorn backend.app.main:app --reload
```

Health endpoint:
- `GET /api/v1/health`

Analyze endpoint:
- `POST /api/v1/analyze`
